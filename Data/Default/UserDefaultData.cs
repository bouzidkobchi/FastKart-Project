using Microsoft.EntityFrameworkCore;

namespace FastKart.Data.Default
{
    public class UserDefaultData
    {
        public static ModelBuilder ApplyUserDefaultData(ModelBuilder modelBuilder)
        {
            return modelBuilder;
        }
    }
}
